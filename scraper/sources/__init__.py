"""Data source adapters.

Each sub-package implements a complete data source (listing, detail, history).

Currently available:
- ``aigenis`` — aigenis.by paid source (requires credentials)
- ``moex`` — MOEX ISS public source (no auth required)
Future adapters can be added as new sub-packages.
"""