# QA Audit Report — 2026-08-20

Полный аудит: математика (backend `desk/`, `scoring/`, API), данные (БД/фиды), фронтенд (demo `:8080`, основное приложение `:80`), консоль/сеть/вёрстка.

## 1. Что проверено и в порядке

- `verify-calcs.py` (в контейнере parser): **ALL CHECKS PASSED** — YTM-солвер, нормализация цены, Macaulay/modified duration, convexity, NKD, sanity score-движка.
- Сумма P&L по бумагам на `/demo/stress` (−1371.00 BYN) точно равна общему P&L (−1371 BYN) — детализация консистентна.
- Оптимизатор: веса аллокации в сумме ~100.1%, суммы ордеров (45 772.69 BYN) < капитала (50 000).
- Математика дюраций/спредов на `/demo/desk` корректна (Z-скоры, спреды согласованы с объяснениями).
- Консоль: **0 ошибок/предупреждений** на всех страницах демо (trading, analytics, bond detail, desk, stress, optimizer, portfolio-lab, search) и лендинге.
- Сеть: все запросы 200 (market-data BCSE/MOEX и пр.).
- Вёрстка: нет горизонтального переполнения ни на 490px, ни на 1440px, ни на 2550px; сайдбар на мобильном корректно уходит за экран (off-canvas).
- Валидные проверки MOEX ISS: RU000A0JWTL6 — дата погашения 2026-09-10 корректна, PREVPRICE 102.78, ACCRUEDINT 67.27, COUPONPERCENT 15.25.

## 2. CRITICAL — математика/скоринг

### 2.1. Завышенный скоринг облигаций с честной отрицательной доходностью
**Причина (код):** `scoring/engine.py`, Case 3 (~строки 494–495): при `ytm_pct is None or ytm_pct <= 0` и `coupon_pct > 0` выполняется `ytm_pct = coupon_pct` (пар-фолбэк). Срабатывает даже когда цена есть и честно решённый YTM отрицателен.

**Проявления (демо, 7 подтверждённых бондов):**

| Облигация | Честный YTM | yield_component | Score | Tier |
|---|---|---|---|---|
| Минфин 21C22516 | −21.85% | 7.0 (= купон) | 69.5 | B |
| ЕВРОТОРГ Оп24 (5-200-02-4363) | −11.19% | 10.0 (= купон) | 61.0 | B |
| BCSE-00355 (ТуровМолКомб) | −5.82% | 10.0 | 45.2 | — |
| MF-LB-EUR-0296 | −0.02% | 0.93 | 49.7 | — |
| MF-LB-USD-0265 | −0.67% | 0.88 | 60.7 | — |
| MF-LB-USD-0300 | −0.47% | 0.58 | 58.0 | — |
| 5-200-02-3827 | −3.64% | 7.3 | 52.0 | — |

### 2.2. Скоринг MOEX-бонда посчитан от устаревшей доходности
`MOEX_RU000A0JWTL6` (Почта России БО-03): в одном payload демо-API: `yield_to_maturity = −28.278` (computed_ytm=true, отображается пользователю), но `breakdown.yield_component = 11.74` (устаревшая хранимая +11.7385) → Score 60.74, статус «Нейтральна». Скор и отображаемый YTM противоречат друг другу в одной строке таблицы «Торги».

### 2.3. Калькулятор портфеля подменяет отрицательный YTM купоном
`/demo/portfolio-lab` → вкладка «Калькулятор (свои суммы)»: ЕВРОТОРГ Оп24 в поиске показан с YTM **−11.19%**; после добавления и «Рассчитать портфель» в составе: **YTM 10%** (= купон), «Ожидаемая доходность 10.00%» вместо ≈ −11%. Аналогично для остальных отрицательных YTM (зависит от фида).

**Рекомендация (общая для 2.1–2.3):** убрать пар-фолбэк при наличии цены; при честном решённом YTM ≤ 0 компонент доходности должен быть 0 (или отрицательная штрафная ветка), а не подмена купоном; скор всегда считать от той же доходности, что отображается.

## 3. HIGH — данные

- **3.1. Stale YTM в БД:** RU000A0JWTL6 — хранится +11.7385; MOEX YIELD −26.27; независимый пересчёт −28.28. Причина: `scraper/moex.py` `_quote_and_yield` (~стр. 173+) возвращает `(price, None)` при отрицательном пересчёте, а pipeline сохраняет прежнее значение (`ytm_val or existing.yield_to_maturity`). Итог: основное приложение (API `:8000`) показывает неверные положительные доходности для таких бумаг.
- **3.2. Невозможная YTM у BCSE-бондов:** 5-200-02-4363 (ЕВРОТОРГ Оп24) — хранимая YTM 10.0 = купон; при 43 днях до погашения и цене 102.61% истинная ≈ −10…−11%. У BCSE-бондов `coupon_frequency = NULL` → cross-check YTM в `scraper/sources/aigenis/client.py` пропускается.
- **3.3. Расхождение «скор vs отображаемый YTM»** в одном ответе demo API (см. 2.2) — статус «Нейтральна» при YTM −28%.

## 4. MEDIUM

- **4.1. Placeholder-котировки как рыночные:** 97/345 BCSE-бондов с price = 100.0 ровно и ytm ≈ coupon (MF-LB-BYN / BCSE-00xxx) — неторгуемые бумаги показаны как реальные рыночные (строки «Торги», desk-аномалии).
- **4.2. `maturity_term_text` = «0.1178082191780822»** — сырой float вместо человекочитаемого «0.1 г.» (карточка 5-200-02-4363; в demo детальная карточка тоже отдаёт сырое значение).
- **4.3. Объяснение скоринга противоречит данным:** в карточках 21C22516 и 5-200-02-4363 текст «Доходность к погашению не указана — оценить сложно» при отображаемом YTM −21.85% / −11.19% и yield_component +7 / +10.
- **4.4. Центр-резерв БО-02 (RU000A1056T2):** отображается «52.87% эфф.» при 5 днях до погашения, купоне 18% (freq 12), цене 99.53%. Честная простая годовая ≈ 145%, сложная ≈ 318%. Вероятно, унаследовано из MOEX YIELD без пересчёта — проверить конвенцию/`sane_yield` (tolerance 15 п.п. в `desk/ytm.py`).
- **4.5. UX:** в «Калькуляторе (свои суммы)» ярлык суммы всегда «BYN», даже для USD-облигации (результат при этом «50 000 USD»).

## 5. MINOR

- **5.1. a11y:** `/demo/stress` — 2 замечания: поле формы без label и без id/name.
- **5.2. Данные:** 147/1571 облигаций в БД без цены/доходности (в основном MOEX) — нет ценовых точек на графике «Карта возможностей».
- **5.3. Наст. main app:** «Лучшие облигации» на лендинге берут скоринг из основной БД — туда попадают недостоверные YTM (см. 3.1) и завышенные скоры (см. 2.1), т.к. демо-пересчёт там не применяется.

## 6. Как воспроизвести

```bash
# 2.1 / 2.2 — демо-данные (BCSE + MOEX)
curl http://localhost:8080/api/v1/demo/market-data?market=bcse | jq '.bonds[] | select(.internal_id=="BCSE-00355" or .internal_id=="5-200-02-4363") | {internal_id, yield_to_maturity, score, breakdown}'
curl http://localhost:8080/api/v1/demo/market-data?market=moex | jq '.bonds[] | select(.internal_id=="MOEX_RU000A0JWTL6") | {yield_to_maturity, score, score_status, breakdown}'
# 2.3 — UI: /demo/portfolio-lab → «Калькулятор (свои суммы)» → найти «ЕВРОТОРГ» → добавить Оп24 → «Рассчитать портфель»
# 3.1 — проверка по источнику
curl "https://iss.moex.com/iss/engines/stock/markets/bonds/boards/TQCB/securities/RU000A0JWTL6.json?iss.meta=off&iss.only=marketdata" | jq '.marketdata.data[0][4]'
```

## 7. Приоритеты исправления

1. `scoring/engine.py` Case 3 — не подменять отрицательный/отсутствующий YTM купоном при наличии цены (2.1, 2.2, 2.3).
2. `scraper/moex.py` `_quote_and_yield` — сохранять отрицательный пересчитанный YTM (3.1).
3. Aigenis-фид: выполнять cross-check YTM даже без `coupon_frequency` (3.2).
4. `maturity_term_text` — форматировать в человекочитаемый вид (4.2); текст объяснения — синхронизировать с фактическим YTM (4.3).

## 8. Статус исправлений (2026-08-20)

Все пункты раздела 7 реализованы и проверены (контейнеры пересобраны, тесты зелёные).

| # | Что сделано | Файл | Проверка |
|---|-------------|------|----------|
| 1 | Case 3 срабатывает только при `price_pct is None`; при наличии цены честный отрицательный YTM даёт `yield_component = 0` | `scoring/engine.py` | 21C22516: score 60.5/B, yc=0.0 (было 69.5/B, yc=7.0=купон) |
| 2 | `_quote_and_yield` возвращает честный отрицательный `computed` (> −99%), pipeline сохраняет его (`solved > -99`) | `scraper/moex.py`, `scraper/pipeline.py` | юнит: YIELD −26.27 → возвращено −23.5915 (было None) |
| 3 | `_sanitize_yield`: дефолт `coupon_frequency = 2`; при расхождении фида с ценой берётся наш честный расчёт, а не сырой фид | `scraper/sources/aigenis/client.py` | юнит: фид 10.0 при цене 102.61 → −11.6562 (было 10.0) |
| 4 | `_humanize_term_text` в парсере и demo-payload (float-лет → «0.12 г.»); `_yield_detail`: отрицательный YTM → «Доходность к погашению отрицательная (X%) — покупка по текущей цене убыточна.» | `scraper/sources/aigenis/api/__init__.py`, `api/demo.py`, `scoring/explain.py` | «0.1178…» → «0.12 г.»; 21C22516/5-200-02-4363 показывают новый текст |
| 5 | Demo market-data: при расхождении предвычисленного score с свежим YTM (детектор `_score_stale`) скор пересчитывается на лету — таблица и карточка не противоречат | `api/demo.py` | RU000A0JWTL6: score 46.0/C, yc=0.0 (было 60.74/B, yc=11.74 при YTM −28.28) |
| 6 | Калькулятор портфеля: `bond_ytm` пересчитывает по цене (как market-data), а не берёт хранимый фид | `portfolio/custom_optimizer.py` | 5-200-02-4363: ytm −11.6628, expected_return −11.66 (было 10.0/10.00) |

Проверено живьём (API): `/market-data` moex+bcse, `/search`, `/bond/{id}`, `/portfolio/custom/calculate`. Поиск: ЕВРОТОРГ Оп24 — ytm −11.66, score 47/C; Оп25 — 8.61, 51.57/C — скор согласован с отображаемой доходностью.

Примечания:
- Хранимые в БД YTM обновятся при следующем запланированном прогоне парсера (фиксы 2–3 действуют при фетче); demo-UI уже честен, т.к. пересчитывает из цены.
- `tests/test_explain.py`, `tests/test_scoring_models_explain.py`, `tests/test_custom_optimizer.py` обновлены под новое поведение и проходят.
- Пре-существующие падения полного прогона (не связаны с этими фиксами): `test_bond_currency_unknown_or_missing_rejected` (падает и на HEAD), `test_norm_currency_aliases` и 2× `test_run_once_moex_*` (тесты не обновлены под незакоммиченные правки валютного списка из прошлой сессии — pipeline теперь опрашивает 6 валют, мок даёт 2).

## 9. Риск-модель по эмитентам (2026-08-20)

Задача Мирослава: 1) дифференцированный риск-скор по каждому эмитенту из таблицы котировок BCSE на основе конкретных публичных данных; 2) пересобрать контейнеры и спарсить новые облигации; 3) учесть внешние данные, которых нет в приложении.

### Что сделано

| # | Что | Файл | Проверка |
|---|-----|------|----------|
| 1 | Реестр 20 кредитных профилей эмитентов (`IssuerProfile`: алиасы, вид, credit-компонент, обоснование, источники) + нормализация имён + `profile_risk_ladder` (score/level) | `scoring/issuer_risk.py` (новый) | `tests/test_issuer_risk.py` (новый, зелёный) |
| 2 | `_credit_risk_component`: профиль эмитента приоритетнее tier-классификации; статусные штрафы (defaulted/delisted −28/−12) перекрывают профиль | `scoring/engine.py` | тесты: НП-СЕРВИС 36, АВАНГАРД 50, Минфин 90, defaulted → 15 |
| 3 | Demo-payload: для профильных эмитентов `issuer_risk` возвращает `issuer_profile`, `profile_kind`, `sources` + обоснование (basis); legacy-ветки и сигнатура не тронуты | `api/demo.py` | `tests/test_issuer_risk.py` (demo-payload) |
| 4 | Сбербанк: 3.0 → 4.0 (tier bank_systemic) по МСФО 2025 (прибыль 245 млн BYN, капитал 1,42 млрд BYN) | `scoring/issuer_risk.py` | тесты обновлены (3.0 → 4.0) |
| 5 | Профиль Нацбанка РБ добавлен после live-проверки (попадал в банки 62): кредит 10.0, алиасы «национальный банк»/«нбрб», вид sovereign | `scoring/issuer_risk.py` | NB-SB-BYN-0260: 90/Очень низкий, profile=nbrb |
| 6 | Исправлена граница лэддера (`>= -2`/`>= -5` → `> -2`/`> -5`): −5.0 попадал в 44 вместо 36 | `scoring/issuer_risk.py` | ladder(−5.0) = 36/Высокий |
| 7 | Пересборка контейнеров (parser/api/bot healthy), свежий скрейп БВФБ (listing 338, details 337, scored 1572) и MOEX (1210/1210), рескоринг | Docker, `scraper` CLI | `fetched_at` 2026-08-20T08:00 UTC |

### Профили (credit-компонент → скор)

| Эмитент | Credit | Скор/Уровень | Источник обоснования |
|---------|--------|--------------|----------------------|
| Минфин РБ | 12.0 | 90 / Очень низкий | суверен, бюджетные гарантии |
| Нацбанк РБ | 10.0 | 90 / Очень низкий | инструмент ДКП, эмитент без кредитного риска частного сектора |
| Исполкомы (обл/рай) | 8.0 | 82 / Очень низкий | обязательства обеспечены бюджетом уровня |
| Сбер Банк | 4.0 | 68 / Умеренно низкий | МСФО 2025 |
| Альфа-Банк / Банк Решение / МТБанк / Евроторг | 2.0–3.0 | 62 / Умеренно низкий | рейтинги BIK Ratings, отчётность МСФО |
| Айгенис / Дженерал лизинг | 1.0–1.5 | 58 / Умеренный | брокер, лизинговая компания |
| Гурмина | 0.0 | 56 / Умеренный | производство, свежая отчётность |
| Активлизинг / Авангард лизинг / Чистый берег / Оливер / МостраГрупп | −0.5…−1.0 | 50 / Повышенный | кредитное качество, кэш-флоу, ограниченная свежая отчётность |
| ВЛК / Хольцгрупп / Бутик-Инвест | −1.5…−2.0 | 50 / Повышенный | 2022 год, отраслевой стресс |
| НП-Сервис | −5.0 | 36 / Высокий | дефолт 2022 |
| Без профиля | — | 56 / Умеренный (tier) | legacy-классификация |

Полный список алиасов и basis — в `scoring/issuer_risk.py`. Матчинг: нормализация (lowercase, срез ООО/ОДО/ЗАО/ОАО, скобки/кавычки) + подстрока по алиасам; поведение неизвестных эмитентов не изменилось.

### Проверено живьём

- `/api/v1/demo/market-data` (345 bonds, 83 эмитента): НП-СЕРВИС 36/Высокий, Бутик-Инвест 44, АВАНГАРД/ОЛИВЕР/Чистый берег/ВЛК/МОСТРА/Хольцгрупп/Активлизинг 50, без профиля 56, Гурмина 56 (профиль 0.0), Айгенис/Дженерал лизинг 58, МТБанк/ЕВРОТОРГ/Альфа/Банк Решение 62, Сбер 68, ВТБ/Белгазпромбанк/Беларусбанк 68 (tier), исполкомы 82, Минфин 90, Нацбанк 90.
- demo-UI (`http://localhost:8080/demo`): таблица показывает level/score по каждому эмитенту; «Айгенис Оп33» → 82 (эмитент в БД — Могилёвский облисполком, Айгенис агент выпуска — корректно).

### Предположения и ограничения

- ОЛИВЕР, ВЛК, МостраГрупп: нет свежих финанов — кредит по данным 2022 г. и отраслевому стрессу.
- НП-Сервис: дефолт 2022 (публичный факт), профиль −5.0.
- Одна ошибка скрейпа: UniqueViolation `uq_bonds_isin` BYM000002154 (дубликат ISIN с другим internal_id в листинге) — не критично, залогировано в lineage; повторный прогон возьмёт свежую запись.
- Скоры-в-БД обновятся при штатном прогоне парсера; demo-пересчёт на лету уже использует профили.